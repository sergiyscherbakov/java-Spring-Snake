# 🐍 Гра Змійка на Spring Boot

![Java](https://img.shields.io/badge/Java-17-orange?style=flat&logo=java) ![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.0-brightgreen?style=flat&logo=springboot) ![Maven](https://img.shields.io/badge/Maven-3.6+-blue?style=flat&logo=apachemaven) ![WebSocket](https://img.shields.io/badge/WebSocket-STOMP-red?style=flat) ![License](https://img.shields.io/badge/License-MIT-yellow?style=flat)

## Автор

**Розробник:** Сергій Щербаков
**Email:** sergiyscherbakov@ukr.net
**Telegram:** @s_help_2010

### 💰 Підтримати розробку
Задонатити на каву USDT (BINANCE SMART CHAIN):
**`0xDFD0A23d2FEd7c1ab8A0F9A4a1F8386832B6f95A`**

---

Класична гра "Змійка" реалізована за допомогою Java Spring Boot з використанням WebSocket для оновлень в реальному часі.

## 📋 Опис

Проста та захоплююча гра "Змійка" з красивим веб-інтерфейсом. Гра працює в браузері та використовує WebSocket для комунікації між клієнтом та сервером.

## 🚀 Технології

- **Backend**: Java 17, Spring Boot 3.2.0
- **Frontend**: HTML5, CSS3, JavaScript
- **Комунікація**: WebSocket (STOMP)
- **Шаблонізатор**: Thymeleaf
- **Збірка**: Maven

## 📦 Вимоги

- Java 17 або вище
- Maven 3.6+

## 🛠️ Встановлення та запуск

### Клонування репозиторію

```bash
git clone https://github.com/sergiyscherbakov/java-Spring-Snake.git
cd java-Spring-Snake
```

### Запуск за допомогою Maven

```bash
mvn spring-boot:run
```

### Запуск скомпільованого JAR

```bash
mvn clean package
java -jar target/snake-game-1.0.0.jar
```

Додаток буде доступний за адресою: **http://localhost:8080**

## 🎮 Як грати

1. Відкрийте браузер та перейдіть на http://localhost:8080
2. Натисніть кнопку **"Почати гру"**
3. Використовуйте клавіші керування:
   - **⬆️ Стрілка вгору** або **W** - рух вгору
   - **⬇️ Стрілка вниз** або **S** - рух вниз
   - **⬅️ Стрілка вліво** або **A** - рух вліво
   - **➡️ Стрілка вправо** або **D** - рух вправо
4. Збирайте червоні яблука для збільшення рахунку
5. Уникайте стін та власного хвоста!

## 📁 Структура проекту

```
java-Spring-Snake/
├── src/
│   └── main/
│       ├── java/
│       │   └── ua/snake/game/
│       │       ├── SnakeGameApplication.java
│       │       ├── config/
│       │       │   └── WebSocketConfig.java
│       │       ├── controller/
│       │       │   └── GameController.java
│       │       ├── model/
│       │       │   ├── Direction.java
│       │       │   ├── Food.java
│       │       │   ├── GameState.java
│       │       │   ├── Point.java
│       │       │   └── Snake.java
│       │       └── service/
│       │           └── GameService.java
│       └── resources/
│           ├── static/
│           │   ├── css/
│           │   │   └── style.css
│           │   └── js/
│           │       └── game.js
│           ├── templates/
│           │   └── index.html
│           └── application.properties
├── pom.xml
└── README.md
```

## 📚 Детальний опис класів та методів

### 🎯 SnakeGameApplication.java
**Призначення:** Головний клас додатку Spring Boot, точка входу програми.

#### Методи:
- **`main(String[] args)`**
  - **Опис:** Запускає Spring Boot додаток
  - **Параметри:**
    - `args` - аргументи командного рядка

---

### ⚙️ config/WebSocketConfig.java
**Призначення:** Конфігураційний клас для налаштування WebSocket з'єднань.

#### Методи:
- **`configureMessageBroker(MessageBrokerRegistry config)`**
  - **Опис:** Налаштовує брокер повідомлень для WebSocket
  - **Параметри:**
    - `config` - реєстр конфігурації брокера повідомлень
  - **Функціонал:**
    - Встановлює префікс `/topic` для підписок
    - Встановлює префікс `/app` для повідомлень від клієнта

- **`registerStompEndpoints(StompEndpointRegistry registry)`**
  - **Опис:** Реєструє STOMP endpoints для WebSocket з'єднань
  - **Параметри:**
    - `registry` - реєстр для додавання endpoints
  - **Функціонал:**
    - Створює endpoint `/ws-snake`
    - Дозволяє всі origins
    - Додає підтримку SockJS для браузерів без WebSocket

---

### 🎮 controller/GameController.java
**Призначення:** Контролер для обробки HTTP та WebSocket запитів гри.

#### Методи:
- **`index()`**
  - **Опис:** Повертає головну сторінку гри
  - **Повертає:** назву шаблону Thymeleaf "index"

- **`startGame(@DestinationVariable String sessionId)`**
  - **Опис:** Створює нову гру для вказаної сесії
  - **Параметри:**
    - `sessionId` - унікальний ідентифікатор ігрової сесії
  - **Повертає:** новий стан гри `GameState`

- **`changeDirection(@DestinationVariable String sessionId, String direction)`**
  - **Опис:** Змінює напрямок руху змійки
  - **Параметри:**
    - `sessionId` - ідентифікатор ігрової сесії
    - `direction` - новий напрямок руху (UP, DOWN, LEFT, RIGHT)
  - **Функціонал:** Валідує і застосовує зміну напрямку

- **`restartGame(@DestinationVariable String sessionId)`**
  - **Опис:** Перезапускає гру для вказаної сесії
  - **Параметри:**
    - `sessionId` - ідентифікатор ігрової сесії
  - **Повертає:** новий стан гри після перезапуску

---

### 📊 model/Point.java
**Призначення:** Представляє точку на ігровому полі з координатами x та y.

#### Поля:
- `x` - координата X (горизонтальна позиція)
- `y` - координата Y (вертикальна позиція)

#### Методи:
- **`equals(Object obj)`**
  - **Опис:** Перевіряє рівність двох точок за координатами
  - **Параметри:**
    - `obj` - об'єкт для порівняння
  - **Повертає:** `true` якщо координати співпадають

- **`hashCode()`**
  - **Опис:** Генерує хеш-код на основі координат
  - **Повертає:** унікальний хеш-код для точки

---

### 🧭 model/Direction.java
**Призначення:** Enum для представлення напрямків руху змійки.

#### Константи:
- `UP(0, -1)` - рух вгору
- `DOWN(0, 1)` - рух вниз
- `LEFT(-1, 0)` - рух вліво
- `RIGHT(1, 0)` - рух вправо

#### Поля:
- `dx` - зміщення по осі X
- `dy` - зміщення по осі Y

#### Методи:
- **`getDx()`**
  - **Опис:** Повертає зміщення по осі X
  - **Повертає:** значення dx

- **`getDy()`**
  - **Опис:** Повертає зміщення по осі Y
  - **Повертає:** значення dy

- **`opposite()`**
  - **Опис:** Повертає протилежний напрямок
  - **Повертає:** протилежний напрямок (UP↔DOWN, LEFT↔RIGHT)

---

### 🐍 model/Snake.java
**Призначення:** Модель змійки з логікою руху та перевірки зіткнень.

#### Поля:
- `body` - список точок, що представляють тіло змійки
- `direction` - поточний напрямок руху

#### Методи:
- **`Snake()`**
  - **Опис:** Конструктор, ініціалізує змійку початковою довжиною 3 сегменти
  - **Початкова позиція:** (10,10), (9,10), (8,10)
  - **Початковий напрямок:** RIGHT

- **`getHead()`**
  - **Опис:** Повертає голову змійки (перший елемент)
  - **Повертає:** точку `Point` голови змійки

- **`move(boolean grow)`**
  - **Опис:** Рухає змійку в поточному напрямку
  - **Параметри:**
    - `grow` - якщо `true`, змійка збільшується (з'їла їжу), якщо `false` - довжина залишається незмінною
  - **Функціонал:**
    - Додає нову голову в напрямку руху
    - Видаляє хвіст, якщо не потрібно рости

- **`checkSelfCollision()`**
  - **Опис:** Перевіряє чи зіткнулася голова змійки з власним тілом
  - **Повертає:** `true` якщо є зіткнення з собою

- **`changeDirection(Direction newDirection)`**
  - **Опис:** Змінює напрямок руху змійки
  - **Параметри:**
    - `newDirection` - новий напрямок руху
  - **Функціонал:** Запобігає руху в протилежному напрямку

---

### 🍎 model/Food.java
**Призначення:** Представляє їжу на ігровому полі.

#### Поля:
- `position` - позиція їжі на полі (об'єкт `Point`)

---

### 🎲 model/GameState.java
**Призначення:** Управляє повним станом гри та її логікою.

#### Поля:
- `snake` - об'єкт змійки
- `food` - об'єкт їжі
- `score` - поточний рахунок гравця
- `gameOver` - чи закінчена гра
- `gridWidth` - ширина ігрового поля (30 клітинок)
- `gridHeight` - висота ігрового поля (20 клітинок)

#### Методи:
- **`GameState()`**
  - **Опис:** Конструктор, ініціалізує нову гру
  - **Функціонал:**
    - Створює нову змійку
    - Генерує першу їжу
    - Встановлює рахунок 0

- **`generateFood()`**
  - **Опис:** Генерує їжу в випадковій позиції
  - **Функціонал:**
    - Випадкова позиція в межах поля
    - Перевіряє, що їжа не з'являється на тілі змійки

- **`update()`**
  - **Опис:** Оновлює стан гри (викликається кожні 150ms)
  - **Функціонал:**
    - Перевіряє чи з'їсть змійка їжу
    - Рухає змійку
    - Збільшує рахунок (+10 за кожну їжу)
    - Генерує нову їжу після поїдання
    - Перевіряє зіткнення зі стінами
    - Перевіряє зіткнення з власним тілом
    - Встановлює `gameOver = true` при зіткненні

---

### 🔧 service/GameService.java
**Призначення:** Сервісний клас для управління множинними ігровими сесіями.

#### Поля:
- `messagingTemplate` - шаблон для відправки WebSocket повідомлень
- `games` - карта ігрових сесій (sessionId → GameState)

#### Методи:
- **`GameService(SimpMessagingTemplate messagingTemplate)`**
  - **Опис:** Конструктор з ін'єкцією залежності
  - **Параметри:**
    - `messagingTemplate` - Spring шаблон для WebSocket повідомлень

- **`createGame(String sessionId)`**
  - **Опис:** Створює нову гру для сесії
  - **Параметри:**
    - `sessionId` - унікальний ідентифікатор сесії
  - **Повертає:** новий об'єкт `GameState`

- **`getGame(String sessionId)`**
  - **Опис:** Отримує гру за ідентифікатором сесії
  - **Параметри:**
    - `sessionId` - ідентифікатор сесії
  - **Повертає:** об'єкт `GameState` або `null`

- **`changeDirection(String sessionId, Direction direction)`**
  - **Опис:** Змінює напрямок змійки в грі
  - **Параметри:**
    - `sessionId` - ідентифікатор сесії
    - `direction` - новий напрямок руху
  - **Функціонал:** Змінює напрямок тільки якщо гра активна

- **`restartGame(String sessionId)`**
  - **Опис:** Перезапускає гру для сесії
  - **Параметри:**
    - `sessionId` - ідентифікатор сесії
  - **Функціонал:** Створює новий `GameState` для існуючої сесії

- **`updateGames()`**
  - **Опис:** Автоматично оновлює всі активні ігри
  - **Анотація:** `@Scheduled(fixedRate = 150)` - викликається кожні 150ms
  - **Функціонал:**
    - Проходить по всіх активних іграх
    - Оновлює стан кожної гри
    - Відправляє оновлення клієнтам через WebSocket

- **`removeGame(String sessionId)`**
  - **Опис:** Видаляє гру з пам'яті
  - **Параметри:**
    - `sessionId` - ідентифікатор сесії для видалення

---

### 🎨 resources/static/css/style.css
**Призначення:** Стилі для веб-інтерфейсу гри.
- Градієнтний фон
- Адаптивний дизайн
- Стилізовані кнопки та модальні вікна
- Анімації

### 🎯 resources/static/js/game.js
**Призначення:** JavaScript клієнт для взаємодії з сервером.
- WebSocket підключення через SockJS/STOMP
- Canvas рендеринг ігрового поля
- Обробка клавіатури (стрілки та WASD)
- Відображення змійки та їжі
- Управління станом гри

### 📄 resources/templates/index.html
**Призначення:** HTML шаблон головної сторінки.
- Canvas для відображення гри
- Кнопки управління
- Відображення рахунку
- Модальне вікно завершення гри

### ⚙️ resources/application.properties
**Призначення:** Конфігураційний файл Spring Boot.
- Налаштування серверу (порт 8080)
- Конфігурація Thymeleaf
- Налаштування логування

---

## ⚙️ Особливості

- ✅ Реалізація гри на Java з використанням Spring Boot
- ✅ WebSocket комунікація для плавного геймплею
- ✅ Красивий та адаптивний інтерфейс
- ✅ Підтримка декількох одночасних ігрових сесій
- ✅ Підрахунок очок
- ✅ Модальне вікно завершення гри
- ✅ Можливість перезапуску гри

## 📄 Ліцензія

Цей проект ліцензовано під MIT License - дивіться файл [LICENSE](LICENSE) для деталей.